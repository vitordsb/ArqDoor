services:
  frontend:
    build:
      context: .
      target: development
    container_name: arqdoor-frontend-dev
    ports:
      - "5173:5173"
    volumes:
      - ./src:/app/src
      - ./public:/app/public
    env_file:
      - .env.docker.local
    restart: unless-stopped
